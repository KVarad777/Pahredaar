pandas>=2.0
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
anthropic>=0.40.0
faker>=24.0
tqdm>=4.66
matplotlib>=3.7

# --- Blue team / Defend engine ---
scikit-learn>=1.3
lightgbm>=4.1
networkx>=3.1

# --- GNN (install in Colab with the matching CUDA build - see notebooks/03) ---
# torch
# torch_geometric

# --- Sequence model (plain torch is enough, no extra graph libs needed) ---
# torch

# Note: gnn_model.py and sequence_model.py both have a CPU-only, no-torch
# fallback path so the pipeline runs end-to-end even before you install
# torch/torch_geometric - see the docstrings in those files.
